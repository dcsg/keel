#!/usr/bin/env bash
# keel: PreCompact hook — remind Claude to capture state before compaction

echo '⚠️  Context compacting. (1) Update your active plan'"'"'s progress table NOW. (2) Run /keel:session to capture any decisions, constraints, or doc gaps before context is lost.'
