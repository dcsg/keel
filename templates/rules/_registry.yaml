# Keel Rule Registry
# Maps rule names to their template, tier, and paths: glob for .claude/rules/ frontmatter.
# Parent field indicates a framework depends on a language pack.
# Version must match the version: frontmatter in the corresponding template.

# Base (language-agnostic)
code-quality:
  tier: base
  template: base/code-quality.md
  paths: "**/*"
  version: "1.0.0"
  default: true

testing:
  tier: base
  template: base/testing.md
  paths: "**/*"
  version: "1.0.0"
  default: true

security:
  tier: base
  template: base/security.md
  paths: "**/*"
  version: "1.0.0"
  default: true

error-handling:
  tier: base
  template: base/error-handling.md
  paths: "**/*"
  version: "1.0.0"
  default: true

# Base (opt-in)
frontend:
  tier: base
  template: base/frontend.md
  paths: "**/*.{ts,tsx,js,jsx,vue,svelte,css,scss}"
  version: "1.0.0"
  default: false

architecture:
  tier: base
  template: base/architecture.md
  paths: "**/*"
  version: "1.0.0"
  default: false

# Language packs
go:
  tier: lang
  template: lang/go.md
  paths: "**/*.go"
  version: "1.0.0"
  default: false

typescript:
  tier: lang
  template: lang/typescript.md
  paths: "**/*.{ts,tsx}"
  version: "1.0.0"
  default: false

python:
  tier: lang
  template: lang/python.md
  paths: "**/*.py"
  version: "1.0.0"
  default: false

php:
  tier: lang
  template: lang/php.md
  paths: "**/*.php"
  version: "1.0.0"
  default: false

# Framework packs
chi:
  tier: framework
  parent: go
  template: framework/chi.md
  paths: "**/*.go"
  version: "1.0.0"
  default: false

nextjs:
  tier: framework
  parent: typescript
  template: framework/nextjs.md
  paths: "**/*.{ts,tsx,js,jsx}"
  version: "1.0.0"
  default: false

laravel:
  tier: framework
  parent: php
  template: framework/laravel.md
  paths: "**/*.php"
  version: "1.0.0"
  default: false

symfony:
  tier: framework
  parent: php
  template: framework/symfony.md
  paths: "**/*.php"
  version: "1.0.0"
  default: false

rails:
  tier: framework
  parent: ruby
  template: framework/rails.md
  paths: "**/*.rb"
  version: "1.0.0"
  default: false

django:
  tier: framework
  parent: python
  template: framework/django.md
  paths: "**/*.py"
  version: "1.0.0"
  default: false
